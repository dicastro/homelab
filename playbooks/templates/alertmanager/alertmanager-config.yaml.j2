global:
  resolve_timeout: 5m

route:
  receiver: 'default'

receivers:
  - name: 'default'
    # Example: Send email, Slack, webhook, etc.
    # email_configs:
    #   - to: 'you@example.com'
    # slack_configs:
    #   - channel: '#alerts'

inhibit_rules: []